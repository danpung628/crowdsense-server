# =============================================================================
# EC2 환경용 환경변수 설정 예시
# 이 파일을 .env로 복사해서 EC2 환경에 맞게 수정하세요
# =============================================================================

# Server Configuration
PORT=3000

# 환경 모드 (EC2에서는 ec2로 설정)
ENVIRONMENT_MODE=ec2

# MongoDB Configuration (EC2 내부 MongoDB 사용)
# EC2 인스턴스에 MongoDB를 직접 설치하여 사용하는 경우
MONGODB_URI=mongodb://127.0.0.1:27017/crowdsense

# Redis Configuration (EC2 내부 Redis 사용)  
# EC2 인스턴스에 Redis를 직접 설치하여 사용하는 경우
REDIS_URL=redis://127.0.0.1:6379

# JWT Configuration
JWT_SECRET=your-production-secret-key-change-this
JWT_REFRESH_SECRET=your-production-refresh-secret-key-change-this

# Development Flag (운영환경에서는 0으로 설정 - 인증 필요)
DEV_FLAG=0

# Seoul Open API Configuration
SEOUL_API_KEY=47464b765073696c33366142537a7a
SEOUL_POPULATION_API_URL=http://openapi.seoul.go.kr:8088
SEOUL_SUBWAY_API_KEY=your-subway-api-key
SEOUL_PARKING_API_KEY=your-parking-api-key

# AWS Configuration
AWS_REGION=ap-northeast-2
AWS_S3_BUCKET_NAME=crowdsense-data-bucket

# AWS 자격증명 (EC2에서는 IAM 역할 사용 권장)
# EC2 인스턴스에 IAM 역할을 연결하면 아래 키들은 설정하지 않아도 됩니다
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key

# =============================================================================
# EC2 설정 가이드
# =============================================================================
# 1. EC2 IAM 역할 생성:
#    - S3FullAccess 또는 필요한 S3 권한만 포함
#    - EC2 인스턴스에 역할 연결
# 
# 2. S3 버킷 생성:
#    - 버킷명: crowdsense-data-bucket (또는 원하는 이름)
#    - 리전: ap-northeast-2 (서울)
#
# 3. MongoDB 설치 (Ubuntu 기준):
#    sudo apt update
#    sudo apt install -y mongodb
#    sudo systemctl start mongodb
#    sudo systemctl enable mongodb
#
# 4. Redis 설치 (Ubuntu 기준):
#    sudo apt update  
#    sudo apt install -y redis-server
#    sudo systemctl start redis-server
#    sudo systemctl enable redis-server
#
# 5. Node.js 설치:
#    curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
#    sudo apt-get install -y nodejs
#
# 6. 애플리케이션 배포:
#    git clone <repository>
#    cd crowdsense-server/server
#    npm install
#    cp env.ec2.example .env
#    # .env 파일을 실제 값으로 수정
#    npm start
# =============================================================================
