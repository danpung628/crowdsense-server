# 작업 확인 체크리스트

지금까지 한 작업이 잘 되었는지 확인하는 체크리스트입니다.

## ✅ 1. 빌드 확인

### 빌드 성공 확인
```bash
cd client
npm run build
```

**확인 사항:**
- ✅ 빌드가 에러 없이 완료되는가?
- ✅ `dist/` 폴더가 생성되는가?
- ✅ 코드 스플리팅이 작동하는가? (각 페이지가 별도 파일로 분리됨)

**현재 상태:** ✅ 빌드 성공 (3.44초 소요)

---

## ✅ 2. 개발 서버 실행 확인

### 개발 서버 실행
```bash
cd client
npm run dev
```

**확인 사항:**
- ✅ 서버가 `http://localhost:5173`에서 실행되는가?
- ✅ 브라우저에서 페이지가 정상적으로 로드되는가?
- ✅ 콘솔에 에러가 없는가?

**확인 방법:**
1. 브라우저 개발자 도구 열기 (F12)
2. Console 탭에서 에러 확인
3. Network 탭에서 API 요청 확인

---

## ✅ 3. 환경 변수 확인

### 환경 변수 파일 확인
```bash
# Windows PowerShell
Get-Content client\.env
Get-Content client\.env.production

# Linux/Mac
cat client/.env
cat client/.env.production
```

**확인 사항:**
- ✅ `.env` 파일이 존재하는가?
- ✅ `.env.production` 파일이 존재하는가?
- ✅ `VITE_API_BASE_URL`이 올바르게 설정되어 있는가?

**현재 상태:**
- ✅ `.env`: `VITE_API_BASE_URL=http://localhost:3000/api`
- ✅ `.env.production`: `VITE_API_BASE_URL=https://YOUR_API_GATEWAY_URL/api`

### 환경 변수 로드 확인
브라우저 콘솔에서:
```javascript
console.log(import.meta.env.VITE_API_BASE_URL)
```
예상 결과: `http://localhost:3000/api` (개발 환경)

---

## ✅ 4. 코드 스플리팅 확인

### 빌드 결과 확인
```bash
# 빌드 후 dist 폴더 확인
ls dist/assets/  # Linux/Mac
dir dist\assets\  # Windows
```

**확인 사항:**
- ✅ 각 페이지가 별도 파일로 분리되었는가?
  - `Home-*.js`
  - `CrowdMap-*.js`
  - `CrowdDetail-*.js`
  - `Transit-*.js`
  - `Parking-*.js`
  - `PopularPlaces-*.js`
  - `HistoryView-*.js`

**현재 상태:** ✅ 코드 스플리팅 작동 확인됨

---

## ✅ 5. PWA 기능 확인

### Service Worker 등록 확인
1. 개발 서버 실행 (`npm run dev`)
2. 브라우저 개발자 도구 열기 (F12)
3. Application 탭 → Service Workers 확인

**확인 사항:**
- ✅ Service Worker가 등록되어 있는가?
- ✅ 상태가 "activated"인가?

### Manifest 확인
1. Application 탭 → Manifest 확인

**확인 사항:**
- ✅ Manifest가 로드되는가?
- ✅ 앱 이름이 "CrowdSense"인가?
- ✅ 아이콘이 표시되는가?

### 오프라인 테스트
1. 개발 서버 실행
2. 브라우저에서 페이지 로드
3. 개발자 도구 → Network 탭 → "Offline" 체크
4. 페이지 새로고침

**확인 사항:**
- ✅ 오프라인에서도 기본 페이지가 표시되는가? (캐시된 버전)

---

## ✅ 6. 반응형 디자인 확인

### 브라우저 개발자 도구 사용
1. 개발 서버 실행
2. F12 → Toggle device toolbar (Ctrl+Shift+M)
3. 다양한 화면 크기로 테스트

**확인 사항:**
- ✅ 모바일 (375px): 레이아웃이 적절한가?
- ✅ 태블릿 (768px): 레이아웃이 적절한가?
- ✅ 데스크톱 (1920px): 레이아웃이 적절한가?
- ✅ Navbar가 반응형으로 작동하는가? (모바일에서 텍스트 숨김)

**테스트할 페이지:**
- 홈 (`/`)
- 인파 (`/crowd`)
- 교통 (`/transit`)
- 주차 (`/parking`)
- 인기장소 (`/popular`)

---

## ✅ 7. API 클라이언트 확인

### 환경 변수 기반 URL 확인
브라우저 콘솔에서:
```javascript
// API 클라이언트가 올바른 URL을 사용하는지 확인
console.log('API Base URL:', import.meta.env.VITE_API_BASE_URL)
```

**확인 사항:**
- ✅ 개발 환경: `http://localhost:3000/api`
- ✅ 프로덕션 빌드: 환경 변수에 따라 변경됨

### API 요청 확인
1. 개발 서버 실행
2. 브라우저 개발자 도구 → Network 탭
3. 페이지에서 API 호출 발생 시도

**확인 사항:**
- ✅ API 요청이 올바른 URL로 전송되는가?
- ✅ 요청 헤더가 올바른가? (`Content-Type: application/json`)
- ✅ 에러 발생 시 적절한 에러 메시지가 표시되는가?

---

## ✅ 8. 에러 처리 확인

### 네트워크 오류 테스트
1. 백엔드 서버를 중지
2. 프론트엔드에서 API 호출 시도

**확인 사항:**
- ✅ 적절한 에러 메시지가 표시되는가?
- ✅ "다시 시도" 버튼이 작동하는가?
- ✅ 사용자 친화적인 메시지인가?

### 타임아웃 테스트
- API 클라이언트의 타임아웃 설정 확인 (15초)
- 타임아웃 발생 시 적절한 메시지 표시 확인

---

## ✅ 9. 라우팅 확인

### 모든 페이지 접근 테스트
1. 개발 서버 실행
2. 각 경로로 직접 접근

**확인 사항:**
- ✅ `/` - 홈 페이지
- ✅ `/crowd` - 인파 목록
- ✅ `/crowd/:areaCode` - 인파 상세
- ✅ `/transit` - 지하철 정보
- ✅ `/parking` - 주차장 정보
- ✅ `/popular` - 인기 장소
- ✅ `/history/:areaCode` - 히스토리

**확인 사항:**
- ✅ 모든 라우트가 정상 작동하는가?
- ✅ 코드 스플리팅으로 인한 로딩 화면이 표시되는가?
- ✅ 404 페이지가 적절히 처리되는가?

---

## ✅ 10. 파일 구조 확인

### 생성된 파일 확인
```bash
# 주요 파일들이 모두 존재하는지 확인
ls client/.env
ls client/.env.production
ls client/deploy.sh
ls client/deploy.ps1
ls client/README.md
ls client/DEPLOYMENT_GUIDE.md
ls client/API_ENDPOINTS.md
```

**확인 사항:**
- ✅ 모든 문서 파일이 존재하는가?
- ✅ 배포 스크립트가 존재하는가?
- ✅ 환경 변수 파일이 존재하는가?

---

## ✅ 11. TypeScript 컴파일 확인

### TypeScript 오류 확인
```bash
cd client
npx tsc --noEmit
```

**확인 사항:**
- ✅ TypeScript 오류가 없는가?
- ✅ 타입 체크가 통과하는가?

---

## ✅ 12. 최종 통합 테스트

### 전체 플로우 테스트
1. 개발 서버 실행 (`npm run dev`)
2. 백엔드 서버 실행 (다른 터미널)
3. 브라우저에서 전체 기능 테스트

**테스트 시나리오:**
1. 홈 페이지 로드 → 통계 표시 확인
2. 인파 페이지 → 지역 목록 확인
3. 특정 지역 클릭 → 상세 페이지 확인
4. 히스토리 확인
5. 지하철 정보 확인
6. 주차장 정보 확인
7. 인기 장소 확인

**확인 사항:**
- ✅ 모든 기능이 정상 작동하는가?
- ✅ 데이터가 올바르게 표시되는가?
- ✅ 에러가 발생하지 않는가?

---

## 📝 확인 결과 기록

각 항목을 확인한 후 체크하세요:

- [ ] 빌드 성공
- [ ] 개발 서버 정상 작동
- [ ] 환경 변수 설정 완료
- [ ] 코드 스플리팅 작동
- [ ] PWA 기능 작동
- [ ] 반응형 디자인 작동
- [ ] API 클라이언트 정상 작동
- [ ] 에러 처리 정상 작동
- [ ] 라우팅 정상 작동
- [ ] 파일 구조 완성
- [ ] TypeScript 오류 없음
- [ ] 전체 통합 테스트 통과

---

## 🐛 문제 발생 시

문제가 발생하면 다음을 확인하세요:

1. **빌드 오류**: `node_modules` 삭제 후 재설치
2. **환경 변수 오류**: `.env` 파일 확인
3. **API 연결 오류**: 백엔드 서버 실행 확인
4. **TypeScript 오류**: 타입 정의 확인

자세한 내용은 `README.md`의 "문제 해결" 섹션을 참고하세요.

