# 작업 확인 체크리스트

지금까지 한 작업이 잘 되었는지 확인하는 체크리스트입니다.

> **참고:** 이 체크리스트는 프론트엔드 개발자가 혼자서 확인할 수 있는 항목들로 구성되어 있습니다.  
> 백엔드 서버가 필요한 항목은 ⚠️ 표시로 구분되어 있습니다.

## 📋 빠른 확인 가이드

### ✅ 혼자서 확인 가능한 항목
- ✅ 1. 빌드 확인
- ✅ 2. 개발 서버 실행 확인
- ✅ 3. 환경 변수 확인
- ✅ 4. 코드 스플리팅 확인
- ✅ 5. PWA 기능 확인
- ✅ 6. 반응형 디자인 확인
- ✅ 7. 라우팅 확인
- ✅ 8. 파일 구조 확인
- ✅ 9. TypeScript 컴파일 확인

## ✅ 1. 빌드 확인

### 빌드 성공 확인
```bash
cd client
npm run build
```

**확인 사항:**
- ✅ 빌드가 에러 없이 완료되는가?
- ✅ `dist/` 폴더가 생성되는가?

**현재 상태:** ✅ 빌드 성공 (3.44초 소요)

---

## ✅ 2. 개발 서버 실행 확인

### 개발 서버 실행
```bash
cd client
npm run dev
```

**확인 사항:**
- ✅ 서버가 `http://localhost:5173`에서 실행되는가?
- ✅ 브라우저에서 **홈 페이지**가 정상적으로 로드되는가?
- ✅ 콘솔에 에러가 없는가?

**확인 방법:**
1. 브라우저에서 `http://localhost:5173` 접속 (홈 페이지)
2. 브라우저 개발자 도구 열기 (F12)
3. Console 탭에서 에러 확인

---

## ✅ 3. 환경 변수 확인

### 환경 변수 파일 확인
```bash
# Windows PowerShell
Get-Content client\.env
Get-Content client\.env.production

# Linux/Mac
cat client/.env
cat client/.env.production
```

**확인 사항:**
- ✅ `.env` 파일이 존재하는가?
- ✅ `.env.production` 파일이 존재하는가?
- ✅ `VITE_API_BASE_URL`이 올바르게 설정되어 있는가?

**현재 상태:**
- ✅ `.env`: `VITE_API_BASE_URL=http://localhost:3000/api`
- ✅ `.env.production`: `VITE_API_BASE_URL=https://YOUR_API_GATEWAY_URL/api`

### 환경 변수 로드 확인
브라우저 콘솔에서:
```javascript
console.log(import.meta.env.VITE_API_BASE_URL)
```
예상 결과: `http://localhost:3000/api` (개발 환경)

---

## ✅ 4. 코드 스플리팅 확인

> **참고:** 코드 스플리팅은 이미 `App.tsx`에 설정되어 있으며, Vite가 자동으로 처리합니다.  
> **다음 경우에만 확인하세요:**
> - 빌드 설정(`vite.config.ts`)을 변경했을 때
> - 새로운 페이지를 추가하고 `lazy()`로 감쌌는지 확인할 때
> - 빌드 결과가 이상할 때 (모든 페이지가 하나의 파일로 합쳐진 경우)

### 빌드 결과 확인 (설정 변경 시에만)
```bash
# 빌드 후 dist 폴더 확인
ls dist/assets/  # Linux/Mac
dir dist\assets\  # Windows
```

**확인 사항:**
- ✅ 각 페이지가 별도 파일로 분리되었는가?
  - `Home-*.js`
  - `CrowdMap-*.js`
  - `CrowdDetail-*.js`
  - `Transit-*.js`
  - `Parking-*.js`
  - `PopularPlaces-*.js`
  - `HistoryView-*.js`

**현재 상태:** ✅ 코드 스플리팅 설정 완료 (자동 작동)

---

## ✅ 5. PWA 기능 확인

### Service Worker 등록 확인
1. 개발 서버 실행 (`npm run dev`)
2. 브라우저 개발자 도구 열기 (F12)
3. Application 탭 → Service Workers 확인

**확인 사항:**
- ✅ Service Worker가 등록되어 있는가?
- ✅ 상태가 "activated"인가?

### Manifest 확인
1. Application 탭 → Manifest 확인

**확인 사항:**
- ✅ Manifest가 로드되는가?
- ✅ 앱 이름이 "CrowdSense"인가?
- ✅ 아이콘이 표시되는가?

### 오프라인 테스트
1. 개발 서버 실행
2. 브라우저에서 페이지 로드
3. 개발자 도구 → Network 탭 → "Offline" 체크
4. 페이지 새로고침

**확인 사항:**
- ✅ 오프라인에서도 기본 페이지가 표시되는가? (캐시된 버전)

---

## ✅ 6. 반응형 디자인 확인

### 브라우저 개발자 도구 사용
1. 개발 서버 실행
2. F12 → Toggle device toolbar (Ctrl+Shift+M)
3. 다양한 화면 크기로 테스트

**확인 사항:**
- ✅ 모바일 (375px): 레이아웃이 적절한가?
- ✅ 태블릿 (768px): 레이아웃이 적절한가?
- ✅ 데스크톱 (1920px): 레이아웃이 적절한가?
- ✅ Navbar가 반응형으로 작동하는가? (모바일에서 텍스트 숨김)

**테스트할 페이지:**
- 홈 (`/`)
- 인파 (`/crowd`)
- 교통 (`/transit`)
- 주차 (`/parking`)
- 인기장소 (`/popular`)

---

## ✅ 7. 라우팅 확인

### 모든 페이지 접근 테스트
1. 개발 서버 실행
2. 각 경로로 직접 접근

**확인 사항:**
- ✅ `/` - 홈 페이지
- ✅ `/crowd` - 인파 목록
- ✅ `/crowd/:areaCode` - 인파 상세 (예: `/crowd/POI001`)
- ✅ `/transit` - 지하철 정보
- ✅ `/parking` - 주차장 정보
- ✅ `/popular` - 인기 장소
- ✅ `/history/:areaCode` - 히스토리 (예: `/history/POI001`)

**확인 사항:**
- ✅ 모든 라우트가 정상 작동하는가? (페이지가 로드되는가?)
- ✅ 코드 스플리팅으로 인한 로딩 화면이 표시되는가?
- ✅ 404 페이지가 적절히 처리되는가? (존재하지 않는 경로 접근 시)
- ⚠️ 데이터 표시는 백엔드 서버가 실행되어야 확인 가능

---

## ✅ 8. 파일 구조 확인

### 생성된 파일 확인
```bash
# 주요 파일들이 모두 존재하는지 확인
ls client/.env
ls client/.env.production
ls client/deploy.sh
ls client/deploy.ps1
ls client/README.md
ls client/DEPLOYMENT_GUIDE.md
ls client/API_ENDPOINTS.md
```

**확인 사항:**
- ✅ 모든 문서 파일이 존재하는가?
- ✅ 배포 스크립트가 존재하는가?
- ✅ 환경 변수 파일이 존재하는가?

---

## ✅ 9. TypeScript 컴파일 확인

### TypeScript 오류 확인
```bash
cd client
npx tsc --noEmit
```

**확인 사항:**
- ✅ TypeScript 오류가 없는가?
- ✅ 타입 체크가 통과하는가?

---

## 📝 확인 결과 기록

각 항목을 확인한 후 체크하세요:

- [ ] 빌드 성공
- [ ] 개발 서버 정상 작동
- [ ] 환경 변수 설정 완료
- [ ] 코드 스플리팅 작동
- [ ] PWA 기능 작동
- [ ] 반응형 디자인 작동
- [ ] 라우팅 정상 작동
- [ ] 파일 구조 완성
- [ ] TypeScript 오류 없음

---

## 🐛 문제 발생 시

문제가 발생하면 다음을 확인하세요:

1. **빌드 오류**: `node_modules` 삭제 후 재설치
2. **환경 변수 오류**: `.env` 파일 확인
3. **API 연결 오류**: 백엔드 서버 실행 확인
4. **TypeScript 오류**: 타입 정의 확인

자세한 내용은 `README.md`의 "문제 해결" 섹션을 참고하세요.

---

## 📌 참고

백엔드 서버가 필요한 확인 항목은 루트 폴더의 `INTEGRATION_CHECKLIST.md` 파일을 참고하세요.
