# CrowdSense 프론트엔드

서울시 인파 밀집도, 지하철 혼잡도, 주차장 정보를 제공하는 스마트시티 웹 애플리케이션의 프론트엔드입니다.

## 🛠️ 기술 스택

- **React 19** + **TypeScript**
- **Vite** (빌드 도구)
- **Tailwind CSS** (스타일링)
- **React Router v7** (라우팅)
- **Axios** (API 통신)
- **PWA** (Progressive Web App) 지원

## 📋 사전 요구사항

- Node.js 18 이상
- npm 또는 yarn

## 🚀 시작하기

### 개발 환경 설정

1. 의존성 설치
```bash
npm install
```

2. 환경 변수 설정
```bash
# .env 파일 생성
VITE_API_BASE_URL=http://localhost:3000/api
```

3. 개발 서버 실행
```bash
npm run dev
```

개발 서버는 `http://localhost:5173`에서 실행됩니다.

### 빌드

프로덕션 빌드를 생성합니다:

```bash
npm run build
```

빌드 결과물은 `dist/` 폴더에 생성됩니다.

### 미리보기

빌드된 결과물을 미리 볼 수 있습니다:

```bash
npm run preview
```

## 🌐 환경 변수

프로젝트 루트에 `.env` 파일을 생성하여 다음 변수를 설정하세요:

```env
# 개발 환경
VITE_API_BASE_URL=http://localhost:3000/api

# 프로덕션 환경 (.env.production)
VITE_API_BASE_URL=https://{api-id}.execute-api.{region}.amazonaws.com/{stage}/api
```

## 📦 배포

### AWS S3 + CloudFront 배포

#### 사전 준비

1. AWS CLI 설치 및 설정
2. 환경 변수 설정:
   - `AWS_S3_BUCKET`: S3 버킷 이름
   - `CLOUDFRONT_DISTRIBUTION_ID`: CloudFront 배포 ID (선택사항)

#### 배포 스크립트 실행

**Windows (PowerShell):**
```powershell
.\deploy.ps1
```

**Linux/Mac:**
```bash
chmod +x deploy.sh
./deploy.sh
```

#### 수동 배포

1. 빌드
```bash
npm run build
```

2. S3 업로드
```bash
aws s3 sync dist/ s3://your-bucket-name/ --delete
```

3. CloudFront 캐시 무효화
```bash
aws cloudfront create-invalidation \
  --distribution-id YOUR_DISTRIBUTION_ID \
  --paths "/*"
```

## 📱 PWA 기능

이 애플리케이션은 PWA(Progressive Web App)로 작동합니다:

- **오프라인 지원**: Service Worker를 통한 캐싱
- **앱 설치**: 모바일 및 데스크톱에서 앱처럼 설치 가능
- **푸시 알림**: 실시간 알림 지원 (구현 예정)

## 🎨 반응형 디자인

- **모바일**: 640px 미만
- **태블릿**: 640px ~ 1024px
- **데스크톱**: 1024px 이상

Tailwind CSS의 반응형 유틸리티 클래스를 사용합니다.

## 📂 프로젝트 구조

```
client/
├── public/           # 정적 파일
│   ├── manifest.json # PWA 매니페스트
│   └── sw.js         # Service Worker
├── src/
│   ├── api/          # API 클라이언트 및 서비스
│   ├── components/    # 재사용 가능한 컴포넌트
│   ├── pages/         # 페이지 컴포넌트
│   ├── App.tsx        # 메인 앱 컴포넌트
│   └── main.tsx       # 진입점
├── dist/              # 빌드 결과물
└── package.json
```

## 🔧 주요 기능

- **인파 모니터링**: 실시간 도심 인파 밀집도 확인
- **교통 정보**: 지하철 실시간 혼잡도 조회
- **주차 정보**: 실시간 주차장 현황 제공
- **인기 장소**: 인파 데이터 기반 인기 장소 랭킹
- **히스토리**: 지역별 인파 데이터 히스토리 조회

## 🐛 문제 해결

### API 연결 오류

1. 백엔드 서버가 실행 중인지 확인
2. `.env` 파일의 `VITE_API_BASE_URL`이 올바른지 확인
3. CORS 설정 확인

### 빌드 오류

1. `node_modules` 삭제 후 재설치:
```bash
rm -rf node_modules package-lock.json
npm install
```

2. TypeScript 오류 확인:
```bash
npm run build
```

## 📝 라이선스

이 프로젝트는 교육 목적으로 제작되었습니다.

## 👥 팀

- **정일혁**: 프론트엔드 개발자

