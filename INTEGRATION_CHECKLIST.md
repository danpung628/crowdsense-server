# 백엔드 서버와 함께 확인하는 체크리스트

> **참고:** 아래 항목들은 백엔드 서버가 실행되어야 확인할 수 있습니다.  
> 프론트엔드 개발자가 혼자서 확인할 필요는 없으며, 백엔드와 함께 통합 테스트 시 확인합니다.

## 📋 확인 항목

### 1. API 요청 확인

**백엔드 서버 실행 필요** (`cd server && npm start`)

**확인 방법:**
1. 개발 서버 실행 (`cd client && npm run dev`)
2. 브라우저 개발자 도구 열기 (F12)
3. Network 탭 클릭
4. 필터에서 "XHR" 또는 "Fetch" 선택 (API 요청만 보기)
5. 페이지 새로고침 (F5)
6. **빨간색으로 표시된 요청이 있는지 확인** (빨간색 = 실패, 모두 회색/초록색 = 정상)

**확인할 항목:**
- ✅ 빨간색으로 표시된 API 요청이 없는가? (빨간색이 없으면 모두 정상)
- ✅ 주요 API 요청들이 보이는가? (`crowds`, `subway`, `parking`)
- ✅ 홈 페이지에서 주요 API들이 자동으로 호출되는가?

**참고:**
- 빨간색이 없으면 모든 API 요청이 성공한 것입니다 (Status 200)
- 빨간색이 있으면 해당 요청을 클릭하여 상세 정보 확인
- 백엔드 서버가 실행되지 않으면 모든 API 요청이 실패(빨간색)로 표시됩니다

---

### 2. API 클라이언트 확인

**백엔드 서버 실행 필요** (`cd server && npm start`)

**확인 방법:**
1. 개발 서버 실행 (`cd client && npm run dev`)
2. 브라우저 개발자 도구 → Network 탭
3. 페이지에서 API 호출 발생 시도

**확인할 항목:**
- ✅ API 요청이 올바른 URL로 전송되는가? (Network 탭 → 요청 클릭 → Headers 탭 확인)
  - 예상 URL: `http://localhost:3000/api/crowds`, `/api/subway`, `/api/parking` 등
- ✅ 요청 헤더가 올바른가? (`Content-Type: application/json`)
- ✅ 에러 발생 시 적절한 에러 메시지가 표시되는가?

---

### 3. 에러 처리 확인

#### 네트워크 오류 테스트
**백엔드 서버 중지 필요** (에러 처리 확인용)

**확인 방법:**
1. 백엔드 서버를 중지 (`cd server`에서 Ctrl+C)
2. 프론트엔드에서 API 호출 시도 (페이지 새로고침)

**확인할 항목:**
- ✅ 적절한 에러 메시지가 표시되는가?
- ✅ "다시 시도" 버튼이 작동하는가? (있는 경우)
- ✅ 사용자 친화적인 메시지인가?

#### 타임아웃 테스트
- API 클라이언트의 타임아웃 설정 확인 (15초) - 코드에서 확인
- 타임아웃 발생 시 적절한 메시지 표시 확인 - 코드에서 확인

---

### 4. 최종 통합 테스트

**백엔드 서버 실행 필요** (`cd server && npm start`)

**확인 방법:**
1. 개발 서버 실행 (`cd client && npm run dev`)
2. 백엔드 서버 실행 (다른 터미널에서 `cd server && npm start`)
3. 브라우저에서 전체 기능 테스트

**테스트 시나리오:**
1. 홈 페이지 로드 → 통계 표시 확인
2. 인파 페이지 → 지역 목록 확인
3. 특정 지역 클릭 → 상세 페이지 확인
4. 히스토리 확인
5. 지하철 정보 확인
6. 주차장 정보 확인
7. 인기 장소 확인

**확인할 항목:**
- ✅ 모든 기능이 정상 작동하는가?
- ✅ 데이터가 올바르게 표시되는가?
- ✅ 에러가 발생하지 않는가?

---

## 📝 확인 결과 기록

각 항목을 확인한 후 체크하세요:

- [ ] API 요청 확인 완료
- [ ] API 클라이언트 확인 완료
- [ ] 에러 처리 확인 완료
- [ ] 최종 통합 테스트 통과

