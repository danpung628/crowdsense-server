# CrowdSense 팀 역할 분담 정리

## 👥 팀원별 역할

### 1. 서성덕 (팀장, 백엔드 개발자)
**담당 업무:** Express 서버, RESTful API, 전체 아키텍처
**주요 로직:**
- ✅ 서버 아키텍처 설계 및 구현 (`server.js`)
- ✅ RESTful API 라우팅 (`routes/`)
- ✅ 비즈니스 로직 (`services/`)
- ✅ 컨트롤러 로직 (`controllers/`)
- ✅ 인증 미들웨어 (`middlewares/authMiddleware.js`)
- ✅ 에러 처리 (`utils/errorHandler.js`)

### 2. 김휘성 (데이터 엔지니어)
**담당 업무:** 공공데이터 연동, 데이터 처리, DB 관리
**주요 로직:**
- ✅ 서울시 API 연동 (`crowdService.js`, `subwayService.js`, `parkingService.js`)
- ✅ 데이터 전처리 및 변환
- ✅ MongoDB 스키마 설계 (`models/`)
- ✅ 데이터 유효성 검증
- ✅ Redis 캐싱 전략 (`utils/redisClient.js`)
- ✅ 데이터 수집 스크립트 (`scripts/`)

### 3. 노원우 (풀스택 개발자)
**담당 업무:** 백엔드 보조, React 웹 개발
**주요 로직:**
- ✅ 백엔드 서비스 로직 보조 (서성덕 지원)
- ✅ React 컴포넌트 개발
- ✅ 프론트엔드 API 통신 (`api/services.ts`, `api/client.ts`)
- ✅ 상태 관리 로직
- ✅ 데이터 바인딩 로직

### 4. 정일혁 (프론트엔드 개발자)
**담당 업무:** React UI/UX, PWA, 실시간 알림
**주요 로직:**
- ✅ React UI/UX 구현 (`pages/`, `components/`)
- ✅ PWA 설정 (`manifest.json`, `sw.js`)
- ✅ 프론트엔드 라우팅 (`App.tsx`, Router)
- ✅ 컨텍스트 API (`contexts/`)
- ✅ UI 로직 및 상태 관리 (useState, useEffect)
- ✅ 프론트엔드 캐싱 및 최적화

---

## 🤔 "로직"의 역할 분담

### 백엔드 로직 (서버 측)
**담당자:** 서성덕, 김휘성, 노원우

1. **비즈니스 로직** (`services/`)
   - 데이터 처리 및 변환 로직
   - 외부 API 호출 및 데이터 통합
   - 캐싱 전략 및 데이터 갱신 로직
   - 인파/교통/주차 데이터 계산

2. **데이터 로직** (`models/`, `utils/`)
   - DB 스키마 정의
   - 데이터 유효성 검증
   - Redis 캐싱 로직
   - 데이터 매핑 및 변환

3. **API 로직** (`controllers/`, `routes/`)
   - RESTful API 엔드포인트
   - 요청/응답 처리
   - 인증 및 권한 처리
   - 에러 핸들링

### 프론트엔드 로직 (클라이언트 측)
**담당자:** 정일혁, 노원우

1. **UI 로직** (`pages/`, `components/`)
   - 컴포넌트 상태 관리
   - 사용자 인터랙션 처리
   - 폼 유효성 검증
   - 이벤트 핸들링

2. **데이터 로직** (`api/`)
   - API 호출 및 데이터 fetch
   - 데이터 변환 및 포맷팅
   - 에러 처리 및 재시도

3. **상태 관리 로직** (`contexts/`)
   - 전역 상태 관리 (Context API)
   - 즐겨찾기 등 개인화 데이터 관리

4. **라우팅 로직** (`App.tsx`)
   - 페이지 네비게이션
   - URL 파라미터 처리

---

## 📊 현재 구현 현황

### ✅ 이미 구현된 로직

1. **백엔드 로직** (완료)
   - Express 서버 구조
   - 6개 API 컨트롤러
   - 6개 비즈니스 서비스
   - MongoDB 모델
   - Redis 캐싱 전략
   - 인증 및 에러 처리

2. **프론트엔드 로직** (완료)
   - 8개 페이지 컴포넌트
   - 3개 API 서비스 레이어
   - 컨텍스트 API (즐겨찾기)
   - 라우팅 로직
   - PWA 설정

### 🚧 추가 구현 가능한 로직

1. **백엔드**
   - Push Notification 서버
   - 더 복잡한 경로 추천 알고리즘
   - 데이터 분석 및 예측 로직

2. **프론트엔드**
   - 더 복잡한 상태 관리 (Redux)
   - 폼 유효성 검증 강화
   - 오프라인 데이터 동기화

---

## 🎯 결론

**"로직"은 다음과 같이 분담됩니다:**

1. **서버 측 로직** → 서성덕(주), 김휘성(데이터), 노원우(보조)
2. **클라이언트 측 로직** → 정일혁(주), 노원우(보조)

**정일혁(프론트엔드)의 로직:**
- ✅ UI 렌더링 로직
- ✅ 사용자 인터랙션 로직
- ✅ 컴포넌트 상태 관리 로직
- ✅ 라우팅 로직
- ✅ API 호출 로직

**정일혁이 담당하지 않는 로직:**
- ❌ 서버 측 비즈니스 로직 (API 처리)
- ❌ 데이터베이스 로직
- ❌ 외부 API 연동 로직

---

## ✅ 정일혁의 역할 요약

**프론트엔드 개발자로서 담당하는 로직:**
1. ✅ React 컴포넌트 로직
2. ✅ UI 상태 관리 로직
3. ✅ 사용자 인터랙션 로직
4. ✅ 라우팅 로직
5. ✅ API 호출 로직 (클라이언트 측)
6. ✅ PWA 오프라인 로직

**모두 구현 완료 ✅**
