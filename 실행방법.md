# CrowdSense 프로젝트 실행 가이드

## 🚀 빠른 실행 방법

### 1. 필수 사전 요구사항

- Node.js (v18 이상 권장)
- MongoDB (localhost:27017 실행 중)
- Redis (localhost:6379 실행 중)

### 2. 서버 실행 (백엔드)

```bash
# 터미널 1번: 서버 실행
cd crowdsense-server/server
npm install        # 의존성 설치 (처음 한 번만)
npm run dev       # 개발 모드 실행
```

**서버가 실행되면:**
- 서버 주소: http://localhost:3000
- API 문서: http://localhost:3000/api-docs

### 3. 웹앱 실행 (프론트엔드)

```bash
# 터미널 2번: 웹앱 실행 (새 터미널 창)
cd crowdsense-web
npm install       # 의존성 설치 (처음 한 번만)
npm run dev      # 개발 모드 실행
```

**웹앱이 실행되면:**
- 웹앱 주소: http://localhost:5173

---

## 📝 상세 설정

### 서버 환경 변수 설정

`crowdsense-server/server/.env` 파일이 필요합니다:

```env
# 서버 포트
PORT=3000

# MongoDB 연결
MONGODB_URI=mongodb://localhost:27017/crowdsense

# Redis 연결
REDIS_URL=redis://127.0.0.1:6379

# JWT 설정
JWT_SECRET=your-secret-key-change-in-production
JWT_REFRESH_SECRET=your-refresh-secret-key-change-in-production

# 개발 모드 (인증 비활성화)
DEV_FLAG=1

# 서울시 API 키
SEOUL_API_KEY=47464b765073696c33366142537a7a
SEOUL_POPULATION_API_URL=http://openapi.seoul.go.kr:8088
SEOUL_SUBWAY_API_KEY=your-subway-api-key
SEOUL_PARKING_API_KEY=your-parking-api-key
```

**env.example 파일이 있다면:**
```bash
cd crowdsense-server/server
cp env.example .env
# .env 파일 수정
```

---

## 🔍 MongoDB & Redis 설치 (아직 설치 안한 경우)

### MongoDB 설치
- Windows: https://www.mongodb.com/try/download/community
- 설치 후 MongoDB 서비스 시작

### Redis 설치 (Windows)
- GitHub에서 Memurai 다운로드: https://github.com/microsoftarchive/redis/releases
- 또는 Memurai 사용: https://www.memurai.com/
- 설치 후 서비스 시작

---

## ✅ 실행 확인

### 서버가 정상적으로 실행되었는지 확인:
- 콘솔에 "🚀 CrowdSense 서버 시작!" 메시지 확인
- http://localhost:3000/api-docs 접속하여 Swagger UI 확인

### 웹앱이 정상적으로 실행되었는지 확인:
- 콘솔에 "Local: http://localhost:5173" 메시지 확인
- 브라우저에서 접속 확인

---

## 🎯 주요 기능 페이지

서버와 웹앱이 모두 실행되면:
- 🏠 **홈**: http://localhost:5173/
- 👥 **인파 밀집도**: http://localhost:5173/crowd
- 🚇 **교통 정보**: http://localhost:5173/transit
- 🅿️ **주차장**: http://localhost:5173/parking
- 🏆 **인기 장소**: http://localhost:5173/popular
- ⭐ **즐겨찾기**: http://localhost:5173/favorites

---

## 🐛 트러블슈팅

### MongoDB 연결 오류
```
Error: MongoDB connection failed
```
**해결**: MongoDB 서비스가 실행 중인지 확인
```powershell
# Windows PowerShell
Get-Service MongoDB
```

### Redis 연결 오류
```
Error: Redis connection failed
```
**해결**: Redis 서비스가 실행 중인지 확인
```powershell
# Redis 실행 확인
redis-cli ping
```

### 포트 충돌
```
Error: listen EADDRINUSE: address already in use :::3000
```
**해결**: 해당 포트를 사용하는 프로세스 종료
```powershell
# 포트 3000 사용 중인 프로세스 종료
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

---

## 📚 추가 정보

- 서버 API 문서: `crowdsense-server/server/API_GUIDE.md`
- 서버 설정 가이드: `crowdsense-server/server/SETUP_GUIDE.md`
- 서버 README: `crowdsense-server/README.md`
